/**
 * Global constants for the CAVE simulator.
 * All units are assumed to be in the original abstract units (e.g., cm).
 * The SCALING_FACTOR converts these units to meters for WebXR compatibility.
 */
export const SCALING_FACTOR = 0.005; // 1 / 100
